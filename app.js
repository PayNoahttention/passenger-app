// initialize the count as 0
// listen for clicks on the increment button
// increment the count variable when the button is clicked
// change the count-el in the HTML

// TODO: BE SURE TO INSTALL QUOKKA AT WORK

let count = 0

function increment() {
    count = count + 1
    console.log(count)
}

increment()